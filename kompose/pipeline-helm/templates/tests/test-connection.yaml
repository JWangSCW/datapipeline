apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pipeline-helm.fullname" . }}-test-connection"
  labels:
    {{- include "pipeline-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pipeline-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
